// Recursion and while-loops.

fn fib(n: Int) -> Int {
    if n < 2 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() {
    let mut i = 0;
    while i <= 10 {
        println(fib(i));
        i = i + 1;
    }
}
