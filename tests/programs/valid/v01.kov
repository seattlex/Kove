// The program from the "Definition of Version 0.1" section of the
// requirements document.

fn add(a: Int, b: Int) -> Int {
    return a + b;
}

fn main() {
    let x = add(10, 20);

    if x > 20 {
        println("Greater than twenty");
    }
}
