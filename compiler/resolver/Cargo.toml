[package]
name = "kove-resolver"
description = "Name resolution for Kove: symbol tables, lexical scopes, and binding every name to what it refers to."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
kove-ast = { workspace = true }
kove-diagnostics = { workspace = true }
