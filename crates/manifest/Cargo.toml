[package]
name = "kove-manifest"
description = "Reading kove.toml and laying out Kove projects. Shared by the CLI, and later by the package manager and language server."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# Intentionally none.
